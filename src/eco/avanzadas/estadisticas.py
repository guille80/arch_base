from eco import suma
def cuadrado(num: int) -> int:
    """Devuelve el cuadrado de un número."""
    return num * num

def suma_secreta(a: int, b: int) -> int:
    """Devuelve la suma de dos números más 42."""
    return a + b + 42